# Session Log - 2026-03-13

## Summary
- Initialized project governance and memory persistence structure.
- Prepared milestone-oriented baseline for implementation.
- Completed M1 skeleton bootstrap for backend services, frontend admin app, frontend SDK package, and local Compose runtime.
- Added shared platform package to standardize `trace_id` propagation and JSON logging baseline.
- Added shared config loader (`configx`) and migrated all service entrypoints to Gin baseline.
- Added local compose runbook, root `Makefile` commands, and accepted ADR for dependency choices.
- Switched Docker Go build proxy source to `.env` (`GOPROXY=https://goproxy.cn,direct`) and kept `.env.example` empty.
- Reverted Dockerfile fixed digest pinning per team preference and verified single-service Docker build success.
- Completed full compose smoke validation (all services started and returned healthy payloads).
- Finalized and published service-level config contract document for M1.
- Fixed review findings: fail-fast Go test aggregation in `Makefile` and Kafka host-client advertised listeners.
- Implemented M2 identity/apikey API skeletons, OpenAPI contracts, migration skeletons, and baseline tests.
- Completed M2 persistence upgrade for identity/apikey: repository abstraction + in-memory default + Postgres repository implementation + startup schema ensure + readiness DB health checks.
- Implemented ingress control-plane validation endpoint (`/v1/control/validate`) with identity token validation, API key validation, and key-owner consistency enforcement.
- Implemented M2 execution-service provider/model management APIs with owner constraints, Postgres persistence, migrations, and tests.
- Implemented M2 routing-service policy management + resolve APIs with owner constraints, Postgres persistence, migrations, and tests.
- Implemented M2 prompt-service template/variable management + render validation APIs with Postgres persistence, migrations, and tests.
- Implemented M2 billing-service pricing/wallet/transaction APIs with Postgres persistence, migrations, owner constraints, and balance checks.
- Added OpenAPI contracts for `execution-service`, `routing-service`, `prompt-service`, and `billing-service` and revalidated full Go module tests.
- Added M2 closeout smoke script (`scripts/agent/m2_closeout_smoke.sh`) and wired Makefile target `m2-smoke`.
- Completed compose-level M2 closeout smoke validation (`make m2-smoke`) across identity/apikey/ingress/execution/routing/prompt/billing.

## Decisions
- Keep constraints as docs-level guidance only.
- Use scaffold-first policy for all new submodules.
- Handle sandbox permission failures by requesting escalation and rerunning original commands (no project-local cache bypass).
- Adopt `gin` for backend HTTP baseline and `caarlos0/env/v11` for env config parsing (ADR-20260313-01 accepted).

## Validation
- `scripts/agent/session_start.sh` -> PASS
- `scripts/agent/check_state.sh` -> PASS
- `go mod tidy` for `backend/packages/platform` and all `backend/services/*` modules -> PASS
- `go test ./...` for `backend/packages/platform` and all `backend/services/*` modules -> PASS
- `docker compose config` -> PASS
- `docker compose build routing-service` with `.env` GOPROXY -> PASS
- `docker compose up -d --build` -> PASS
- `docker exec <service> wget -qO- http://127.0.0.1:8080/healthz` for all 8 services -> PASS
- `docker compose down` -> PASS
- `make test-go` -> PASS (fail-fast loop behavior in place)
- `docker compose config` Kafka listener fields include `INTERNAL://kafka:9092` and `EXTERNAL://localhost:${KAFKA_HOST_PORT}` -> PASS
- `go test ./...` in `backend/services/identity-service` -> PASS
- `go test ./...` in `backend/services/apikey-service` -> PASS
- `go test ./...` in `backend/services/ingress-service` -> PASS
- `go test ./...` in `backend/services/execution-service` -> PASS
- `go test ./...` in `backend/services/routing-service` -> PASS
- `go test ./...` in `backend/services/prompt-service` -> PASS
- `go test ./...` in `backend/services/billing-service` -> PASS
- `make test-go` after M2 prompt/billing updates -> PASS
- `make compose-up` -> PASS
- `make m2-smoke` -> PASS
- `make compose-down` -> PASS
- Escalation used:
  - `pnpm create vue@latest ...` due cache permission (`/home/maxwell/.cache/pnpm/...`)
  - `go test` due `GOCACHE` permission (`/home/maxwell/.cache/go-build/...`)
  - `go mod tidy` due Go cache permission (`/home/maxwell/.cache/go-build/...`)
  - `go get github.com/jackc/pgx/v5...` due sandbox network restriction (`socket: operation not permitted`)
  - `make compose-up` / `make compose-down` due docker socket permission (`/var/run/docker.sock`)
  - `make m2-smoke` due sandbox local socket restriction for curl (`Operation not permitted`)

## Changed Files
- `backend/services/*` (8 service modules, entrypoints, Dockerfiles, layered directories)
- `backend/packages/platform/*`
- `frontend/apps/admin/*`
- `frontend/packages/sdk/*`
- `docker-compose.yml`
- `Makefile`
- `pnpm-workspace.yaml`
- `docs/runbooks/local-compose.md`
- `docs/decisions/ADR-20260313-01-m1-bootstrap-deps.md`
- `contracts/config/service-config-contract.md`
- `contracts/openapi/identity-service-v0.yaml`
- `contracts/openapi/apikey-service-v0.yaml`
- `contracts/openapi/execution-service-v0.yaml`
- `contracts/openapi/routing-service-v0.yaml`
- `contracts/openapi/prompt-service-v0.yaml`
- `contracts/openapi/billing-service-v0.yaml`
- `docs/conventions/m2-permission-test-matrix.md`
- `scripts/agent/m2_closeout_smoke.sh`
- `Makefile` (`m2-smoke`)
- `backend/services/identity-service/internal/infra/postgres/user_repository.go`
- `backend/services/apikey-service/internal/infra/postgres/key_repository.go`
- `backend/services/execution-service/internal/infra/postgres/repository.go`
- `backend/services/routing-service/internal/infra/postgres/repository.go`
- `backend/services/prompt-service/internal/infra/postgres/repository.go`
- `backend/services/billing-service/internal/infra/postgres/repository.go`
- `backend/services/ingress-service/internal/controlplane/*`
- `.env`
- `.env.example`
- `docs/status/current.md`
- `docs/state/project_state.json`

## Next
1. Build M3 ingress orchestration pipeline skeleton (`Auth -> Whitelist -> Template -> Routing -> Execution -> Response`).
2. Implement OpenAI-compatible upstream adapter invocation in execution path.
3. Add E2E tests for explicit concrete model bypass vs custom-model policy routing.
