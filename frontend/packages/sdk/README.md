# @llm-gateway/sdk

Frontend shared SDK skeleton for M1.

## Usage

```ts
import { GatewayClient } from '@llm-gateway/sdk'

const client = new GatewayClient({
  baseURL: 'http://localhost:18080',
  apiKey: 'your-api-key',
})
```
